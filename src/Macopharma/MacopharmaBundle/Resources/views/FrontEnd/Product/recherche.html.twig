<form action="{{ path('product_search') }}" method="POST" class="navbar-form form-search pull-right">
    {{ form_widget(form.recherche) }}
    <button type="submit" class="btn">{{ 'Search'|trans }}</button>
    {{ form_widget(form) }}
</form>